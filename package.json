{
  "name": "feature-flags-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.9"
  }
}
